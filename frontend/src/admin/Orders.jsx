
import React,{useEffect,useState}from"react";import{adminFetch}from"./api";
const statuses=["Pending","Processing","Shipped","Delivered","Cancelled"];
export default function Orders(){const[rows,setRows]=useState([]);const load=()=>adminFetch("/orders").then(d=>setRows(d.data||[]));useEffect(()=>{load()},[]);const change=async(id,Status)=>{await adminFetch(`/orders/${id}/status`,{method:"PUT",body:JSON.stringify({Status})});load()};return <div className="admin-content"><h1 className="admin-title">Orders</h1><p className="admin-subtitle">Update delivery status from your phone or desktop.</p><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Order</th><th>Customer</th><th>Date</th><th>Amount</th><th>Phone</th><th>Status</th></tr></thead><tbody>{rows.map(x=><tr key={x.Id}><td><strong>#{x.Id}</strong></td><td>{x.FullName}</td><td>{new Date(x.OrderDate).toLocaleString("en-IN")}</td><td>₹{Number(x.TotalAmount).toLocaleString("en-IN")}</td><td>{x.PhoneNumber}</td><td><select className="admin-select" value={x.Status} onChange={e=>change(x.Id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div></div>}
